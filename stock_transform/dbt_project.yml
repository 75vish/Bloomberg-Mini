name: 'stock_transform'
version: '1.0.0'
profile: 'stock_transform'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  stock_transform:
    staging:
      +materialized: view
    marts:
      +materialized: table